# Чек-лист выполнения задач DiaNight

- [x] `1. Автоматический Snooze (откладывание тревоги)`
  - [x] Реализовать `autoSnoozeRunnable` и его запуск на 15 минут в `startAlarmSound()`
  - [x] Отмена `autoSnoozeRunnable` при `stopAlarmSound()`
  - [x] Тщательная проверка, что при выключенном колокольчике тревоги заглушены и не могут сработать

- [x] `2. Смещение интерфейса вверх и автоскрытие иконок`
  - [x] Изменить `app:layout_constraintVertical_bias` для `@id/centralClickArea` на `0.38` в `activity_main.xml`
  - [x] Реализовать перехват касаний в `dispatchTouchEvent(MotionEvent ev)` для сброса таймера бездействия
  - [x] Реализовать плавное затухание (fade out) через 15 секунд бездействия для иконки настроек, батареи и колокольчика (если выключен)

- [x] `3. Отображение IoB (Активный инсулин)`
  - [x] Изменить `layoutGlucose` in `activity_main.xml` на `ConstraintLayout`
  - [x] Добавить `@id/tvIoB` справа вверху от `@id/tvGlucose` зеленым цветом `#34C759` и размером `50sp`
  - [x] Парсить поле `iob` в `fetchGlucoseData()` и выводить его на экран в формате `X.XX U` (или скрывать, если данных нет)

- [x] `4. Pixel Shifting (Защита от выгорания OLED)`
  - [x] Создать `pixelShiftHandler` and `Runnable`, запускаемый каждые 2 минуты
  - [x] Смещать `@id/centralClickArea` по осям X и Y на случайную величину от -16 до +16 пикселей

- [x] `5. Частое обновление данных`
  - [x] Изменить задержку сетевого опроса с 60 секунд на 10 секунд в `startNetworkPolling()`

- [x] `6. Отображение процента заряда батареи`
  - [x] Создать векторные иконки `ic_battery.xml` и `ic_battery_charging.xml`
  - [x] Разместить контейнер `@id/ivBatteryContainer` в правом нижнем углу `activity_main.xml`
  - [x] Реализовать `BroadcastReceiver` для обновления уровня заряда и иконки в зависимости от подключения питания

- [x] `7. Логика скрытия пиктограмм (Колокольчик)`
  - [x] Доработать метод `fadeAttributesOut()`: скрывать колокольчик при бездействии только если `alarmEnabled == false`

- [x] `8. Дополнительные настройки для слабоподготовленных пользователей`
  - [x] Создать иконку справки `ic_help.xml` и добавить ее в `SettingsActivity` для открытия диалога со справкой
  - [x] Добавить кнопку/иконку автопоиска IP в `SettingsActivity` рядом с полем IP, реализовать сканирование подсети

- [x] `9. Ребрендинг проекта (Иконка и Название)`
  - [x] Разработать современную плоскую адаптивную векторную иконку (полумесяц + капля + тренд)
  - [x] Переименовать проект в **DiaNight** (изменить strings.xml, settings.gradle, README.md, SettingsActivity.java)

- [x] `10. Улучшение автопоиска мастера xDrip+`
  - [x] Внедрить метод `getLocalIpAddress()` in `SettingsActivity.java` для надежного определения подсети через `NetworkInterface`
  - [x] Настроить `OkHttpClient` с выделенным `Dispatcher` для параллельного выполнения 254 запросов и таймаутом 1.5 секунды
  - [x] Обновить обработку ответа: считать успешным как код 200 (OK), так и код 401 (Unauthorized)
  - [x] Добавить логирование процесса сканирования и понятные уведомления (Toast)
  - [x] Убедиться, что проект успешно собирается
  - [x] Зафиксировать изменения in git

- [x] `11. Тонкая настройка и адаптация под реальные устройства (Android 6.0 и Android 16)`
  - [x] Анализ структуры ответов xDrip+ и нахождение IoB в `bgs[0].iob` эндпоинта `/pebble`
  - [x] Реализация надежного парсинга IoB с заменой европейской запятой на точку (для парсинга типа `"0,64"`)
  - [x] Добавление `android:roundIcon="@mipmap/ic_launcher_round"` в `AndroidManifest.xml`
  - [x] Написание Python-скрипта и генерация квадратных и круглых растровых WebP-иконок для всех density-директорий
  - [x] Повышение `versionCode` до `2` и `versionName` до `"1.1"` для сброса кэша иконки
  - [x] Добавление умного fallback-определения подсети по введенному IP для автопоиска
  - [x] Компиляция и локальное тестирование сборки

- [x] `12. UI-оптимизация отображения глюкозы и IoB`
  - [x] Сближение стрелки тренда сахара путем удаления пробела в формате `"%.1f%s"`
  - [x] Удаление суффикса `" U"` в формате вывода IoB (`"%.2f"`) для экономии экранного пространства
  - [x] Настройка горизонтальной цепочки `packed` в `activity_main.xml` для совместного центрирования сахара и IoB
  - [x] Увеличение отступа и вертикальное выравнивание tvIoB относительно tvGlucose

- [x] `13. Окончательное исправление иконки на Android 16+`
  - [x] Поиск и нахождение скрытого конфликта ресурсов (дефолтный шаблонный `ic_launcher_foreground.xml` в папке `drawable-v24` перекрывал наш кастомный значок на API 24+)
  - [x] Полное удаление конфликтующего файла `drawable-v24/ic_launcher_foreground.xml` через git
  - [x] Пересборка и повторная установка приложения на устройства
  - [x] Проверка отображения новой круглой адаптивной иконки (полумесяц и капля) на экране Android 16

- [x] `14. Новые UI изменения, кликабельные статус-элементы и тест связи`
  - [x] Перенос индикатора батареи `ivBatteryContainer` в верхнюю статус-панель левее предупреждения о сети с уменьшением padding до `8dp`
  - [x] Добавление кликабельности для иконки предупреждения о сети `ivNetworkWarning` с ripple-эффектом и открытием настроек при нажатии
  - [x] Переработка строки ввода IP в настройках: удаление иконки лупы, добавление отступа `8dp` для иконки справки
  - [x] Добавление большой классической кнопки «Автопоиск (Beta)» под текстовыми полями
  - [x] Добавление новой кнопки «Проверить связь» для мгновенного пинга мастера-источника по указанному IP и API Secret
  - [x] Реализация метода `testConnection()` с выводом информативных диалоговых окон (200 OK / 401 Unauthorized / Ошибки)
  - [x] Добавление метода `computeSHA1()` в `SettingsActivity` для проверки связи с зашифрованными мастерами
  - [x] Обновление текста справки с учетом нового расположения кнопок
  - [x] Компиляция и тестирование сборки

- [x] `15. Добавление 5 новых цветов и умное сканирование подсети`
  - [x] Разработка двухрядной панели выбора цвета в `activity_settings.xml` (по 5 кнопок в ряду)
  - [x] Добавление 5 премиальных и комфортных для сна цветов: Янтарный (`#FF9500`), Красный (`#FF3B30`), Пурпурный (`#AF52DE`), Золотой (`#FFCC00`), Бирюзовый (`#30B0C7`)
  - [x] Объявление, инициализация и привязка слушателей для новых кнопок цвета в `SettingsActivity.java`
  - [x] Реализация приоритетного алгоритма subnetting (сначала считывать префикс из IP-поля, затем автоопределение IP, затем fallback)
  - [x] Увеличение OkHttp connect и read таймаутов сканера до `2500 мс` для надежности в зашумленных сетях
  - [x] Проверка сборки и успешное тестирование

- [x] `16. Оптимизация автопоиска (TCP-сканер с SHA-1 авторизацией) и 10 цветов на единой линии в настройках`
  - [x] Удаление 5 последних добавленных цветов и возврат к 10 базовым пресетам
  - [x] Размещение 10 цветов в скролл-ленту HorizontalScrollView слева в одной строке
  - [x] Перенос разделителя, превью и кнопки «Свой цвет» на ту же горизонтальную строку справа
  - [x] Очистка неиспользуемого кода (переменные, findViewById, onClick) в `SettingsActivity.java`
  - [x] Реализация передачи SHA-1 хеша ключа в заголовке `api-secret` при одиночной HTTP-верификации автопоиска в `verifyXdripAndSelect()`
  - [x] Успешная проверка скорости/надежности авторизованного автопоиска пользователем

- [x] `17. Доработки автопоиска, расширение площади экрана и автоинкремент версий`
  - [x] Добавление проверки API Secret перед запуском автопоиска с показом предупреждающего AlertDialog в `SettingsActivity.java`
  - [x] Автоматическое сохранение IP в SharedPreferences при успешном автопоиске
  - [x] Расширение полезной площади экрана: уменьшение боковых отступов `centralClickArea` с `32dp` до `4dp` в `activity_main.xml`
  - [x] Оптимизация ширины `@id/layoutGlucose` в `activity_main.xml` (0dp с constraints)
  - [x] Перенос «Интервала переключения табло» в новую CardView «Настройки табло» ниже в XML
  - [x] Размещение кнопок «Проверить связь», «Автопоиск (Beta)» и большой «Сохранить и выйти» в первой CardView под полем ключа
  - [x] Удаление избыточной нижней панели сохранения в XML и обновление биндингов в Java
  - [x] Создание файла `app/version.properties` со счетчиками версий
  - [x] Написание Gradle-скрипта автоинкремента версии в `app/build.gradle` при каждой сборке
  - [x] Тщательная проверка компиляции, автоинкремента версий и работы интерфейса

- [x] `18. Динамическое масштабирование шрифтов в зависимости от размеров и разрешения экрана`
  - [x] Реализовать метод `adjustTextSizes()` в `MainActivity.java` для вычисления масштаба шрифтов
  - [x] Вызывать `adjustTextSizes()` в `onCreate()` и `onResume()` в `MainActivity.java`
  - [x] Убедиться в корректности компиляции и успешном обновлении версии

- [x] `19. Умное адаптивное масштабирование под длину текста сахара и видимость IoB`
  - [x] Реализовать метод `adjustGlucoseAndIoBTextSizes()` в `MainActivity.java` с учетом длины текста сахара и IoB
  - [x] Добавить вызов `adjustGlucoseAndIoBTextSizes()` в callback-и получения данных в `MainActivity.java`
  - [x] Убедиться в отсутствии обрезания на коротких дисплеях (HD) при трехзначных сахарах и IoB
  - [x] Завершить сборку и проверить инкремент версии с коммитом/пушем

- [x] `20. Дополнительное смещение интерфейса вверх для настольной подставки`
  - [x] Скорректировать `app:layout_constraintVertical_bias` для `@id/centralClickArea` с `0.38` на `0.28` в `activity_main.xml`
  - [x] Проверить сборку приложения и убедиться в инкременте версий

- [x] `21. Отображение версии, разработчика в настройках и обновление документации`
  - [x] Добавить футер в настройки `activity_settings.xml` с динамической версией и инфо о разработчике (Evgeniy Krasnyanskiy, Email)
  - [x] Добавить две независимые кнопки сохранения с уникальными названиями: верхняя «Применить настройки» (быстрое сохранение) и нижняя «Сохранить и выйти» (удобно после скролла)
  - [x] Инициализировать восстановленные поля рингтонов (`tvRingtoneName`, `btnChooseRingtone`, `btnTestRingtone`) и новую нижнюю кнопку `btnSaveBottom` в `initViews()` класса `SettingsActivity.java`
  - [x] Внедрить динамическую загрузку версии через `PackageInfo.versionName` в `SettingsActivity.java`
  - [x] Отредактировать `README.md`: добавить информацию о запуске на старых Android-устройствах 6.0+ (API 23+) с обычными LCD-экранами, дать им вторую прикроватную жизнь и указать email автора для пожеланий
  - [x] Завершить финальную проверку компиляции, Git commit и Git push в репозиторий



